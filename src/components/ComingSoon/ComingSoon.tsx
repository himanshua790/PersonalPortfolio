type Props = {};
import styles from "./ComingSoon.module.scss";

const ComingSoon = (props: Props) => {
  return <section className={styles.container}>Under Construction⚒️</section>;
};

export default ComingSoon;
