import { makeStyles } from "@mui/styles";

export default makeStyles({
  container: {
    display: "flex",
    justifyContent: "flex-end",
    flexDirection: "row",
    alignContent: "center",
    alignItems: "stretch",
  },
});
